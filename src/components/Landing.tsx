import React from "react";
import { Container, Button } from "@mui/material";

const Landing: React.FC<{}> = () => {
    return <Button variant="contained">Landing!</Button>;
};

export default Landing;
